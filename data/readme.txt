这里用于存放本地运行时数据。

该目录下除本文件外，默认不提交到 Git。

常见本地数据包括：

- `router.go`：Dever 初始化生成的路由注册文件
- `load/`：Dever 初始化生成的模型和服务注册文件
- `table/`：本地表结构缓存
- `upload/`：上传文件
- `export/`：导出文件
- `log/`：运行日志
- `project/`：运行时项目文件
